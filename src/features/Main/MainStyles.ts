import { StyleSheet } from 'react-native';

export const mainStyles = StyleSheet.create({
    big: {
      fontSize: 40,
    },
    small: {
      fontSize: 20,
    },
    container: {
      flex: 1
    },
  
    icons: {
      width: 30
    }
  });